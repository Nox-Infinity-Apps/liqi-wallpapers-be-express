export interface IConfig {
  DEFAULT_PORT: number;
  DEBUG_MODE: boolean;
  NODE_ENV: string;
  CONTEXT_PATH: string;
  DB_NAME: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_HOST: string;
}
